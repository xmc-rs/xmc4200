name: Rust

on: 
  push:
    branches:
     - master
     - dev/*
     - dependabot/*
  pull_request:
    branches:
    - master

jobs:
  build:
    strategy:
      matrix:
        rust-channel: [ 'stable', 'nightly' ]
        rust-host: [ 'x86_64-unknown-linux-gnu', 'thumbv7em-none-eabi' ]
    name: ${{ matrix.rust-channel }}-${{ matrix.rust-host }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Install toolchain
        uses: actions-rs/toolchain@v1
        with:
            toolchain: ${{ matrix.rust-channel }}
            target: ${{ matrix.rust-host }}
            override: true
      - name: Cargo Check
        uses: actions-rs/cargo@v1
        with:
          command: check
          args: --verbose
      - name: Cargo Build
        uses: actions-rs/cargo@v1
        with:
          command: build
          args: --verbose
